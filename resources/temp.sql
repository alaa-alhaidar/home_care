SELECT customer_id, AVG(purchase_amount) as avg_purchase
FROM transactions
WHERE purchase_date > '2023-01-01'
GROUP BY customer_id;

(a + b) * (a + b) + (a + b) → 2(a + b) + (a + b) ;

SELECT * FROM X WHERE xi < 1;

